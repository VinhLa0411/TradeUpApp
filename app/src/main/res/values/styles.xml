<resources>
    <!-- Trong res/values/styles.xml -->
    <style name="Label">
        <item name="android:textSize">15sp</item>
        <item name="android:textColor">#333</item>
        <item name="android:layout_width">match_parent</item> <!-- THÊM DÒNG NÀY -->
        <item name="android:layout_height">wrap_content</item> <!-- THÊM DÒNG NÀY -->
    </style>

    <style name="FormEditText">
        <item name="android:background">@drawable/bg_edittext</item>
        <item name="android:paddingLeft">14dp</item>
        <item name="android:paddingRight">14dp</item>
        <item name="android:layout_marginBottom">10dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:minHeight">48dp</item>
    </style>

    <style name="Theme.TradeUp" parent="Theme.MaterialComponents.DayNight.DarkActionBar" />
</resources>
